name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git zip unzip openjdk-17-jdk
          pip install buildozer
      
      - name: Build APK
        run: |
          # Doğrudan buildozer'ı çalıştır, lisansları otomatik kabul etsin
          yes | buildozer android debug || true
          # Tekrar dene (bu sefer lisanslar kabul edilmiş olacak)
          buildozer android debug
      
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: mihrab-apk
          path: bin/*.apk
